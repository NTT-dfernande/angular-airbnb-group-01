import { ConvertToDolarPipe } from './convert-to-dolar.pipe';

describe('ConvertToDolarPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToDolarPipe();
    expect(pipe).toBeTruthy();
  });
});
